# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Whiteout Survival(화이트아웃 서바이벌)의 랠리·개리슨 **편성 계산기**. 리더 3명과 병비를 넣으면
SkillMod 칸 포화 · 위젯 발동 · 병종 전용 스킬 사망 · 조이너 순위 · 시트 카운터/밴드 판정을 계산한다.

- 배포: <https://alhena105.github.io/wos-calc/> (GitHub Pages, main / root)
- 사용자: 한국인 운영자와 **연맹원들**. 본인 계정 전용이 아니라 **남의 조합을 상담**하는 데 쓴다 → 프레임은 항상 계정 불문 일반형으로.
- 해설 문서: <https://blog.astris.kr/posts/f7a87706-3c2f-40c0-b282-d77a311ab4e3> (ko) / <https://blog.astris.kr/posts/adb04f46-d525-46a6-810c-940b72f26c77> (en)

응답은 **한국어**로.

---

## 🔴 이 프로젝트에서 가장 중요한 것

### 1. Ton 시트가 1차 자료다

기준은 TontonYouTube 공개 시트(Heroes, Rallies, Ratios and Joiners)와 그걸 정리한 볼트 가이드다.
**계산 결과가 시트와 엇갈리면 시트를 따르고, 시트가 왜 맞는지 설명한다.** 대안을 밀지 않는다.

시트 주소(원본): <https://docs.google.com/spreadsheets/d/1O62iCo2beZucEUkcFXI7mfWZ-WO9BUCVjCuHlrG5jYo/edit>
게시본(pubhtml): <https://docs.google.com/spreadsheets/d/e/2PACX-1vRAySWt-SGTpyuc8CjSOfpmlpVu1eKkLYc8OGKvWd6znPZ0_pLwQ3JcAC_VXaOgUHzAli-sd3W5-oZV/pubhtml>
**둘은 같은 문서다**(gid 6개가 동일). 2026-09-12 에 세대 탭 54행을 한 줄씩 대조했고 **게시본과 원본이 완전히 같았다** —
게시본이 묵었을 가능성을 의심할 필요는 없다. 다만 읽을 때는 CSV 가 편하다:
`https://docs.google.com/spreadsheets/d/1O62iCo.../gviz/tq?tqx=out:csv&gid=<gid>`
(EN: `Gen 1~9`=29268469 · `Gen 10+`=1604035735 · `Rally Joiners`=974305695)

2026-09-01 확인 시점의 EN 탭은 셋이다 — `Gen 1~9` · **`Gen 10+`(신설)** · **`Rally Joiners`(신설)**.
(FR 탭 셋은 같은 내용의 번역이다.) Gen 13~16 행은 **비어 있고**, 시트 스스로
"12월 업데이트 이후 데이터 부족으로 일부 편성은 최신이 아님"이라고 적어 뒀다.

### 2. 근거가 시트뿐이면 판정도 시트까지만 한다

예전에는 시트 옆에 **3채널 지표(P/T/B)** 라는 자체 판정이 붙어 있었다. 가이드 밴드표에 맞춘
곡선이었는데, 다시 세어 보니 **세 임계값이 각각 밴드 단 한 건에 묶여** 있었다. 화면이 스스로
"이건 메커니즘 모델이 아닙니다 · 지표를 믿지 마세요"라고 적어야 할 정도의 판정이라
**2026-08-22에 통째로 걷어냈다**(`channels` · `TH/THW/FEAS/SUP` · `CH_TXT/CH_FIX/CH_NAME` ·
`modelVerdict`). `unit.mjs` 가 이 심볼들이 되살아나지 않았는지 검사한다.

되살리고 싶으면 **근거부터**다. 밴드표에 점이 더 붙어서 임계값을 한 건이 아니라 여러 건이
묶어야 한다. 실제 병종 스탯으로 시뮬레이션해도 밴드표는 6/12(우연 수준)밖에 재현되지 않으므로,
스탯에서 유도하는 길도 막혀 있다 — 실전은 영웅 SkillMod(2~5배)가 지배하고, 그건 병비가 아니라
슬롯에 들어간 영웅으로 정해진다.

**수비(개리슨)도 같은 표로 판정한다 — 방향만 뒤집는다.** 표의 왼쪽 열이 "상대 방어"라
행 자체가 방어 비율이다. 그래서 수비할 때는 **행을 고르는 키가 내 개리슨**이고 **비교 대상이
들어오는 랠리**다. 들어온 랠리가 내 행의 추천 카운터면 `threat`(정석 카운터가 왔다),
금지 목록이면 `favorable`(상대가 밴드 편성으로 왔다), 아니면 `silent`/`noRow`.
새 가정은 하나도 안 들어간다 — 파생 규칙이 아니라 같은 표의 반대 방향 조회다.

주의: 표의 추천 카운터 중에는 **멀티랠리 전제**가 붙은 것이 있다(60/40 · 60/30/10 행).
정석 카운터 하나가 왔다고 곧바로 지는 게 아니므로, `threat` 표시에 라벨의 전제를 같이 띄운다.

### 3. 정정 이력 — 같은 실수를 반복하지 말 것

전부 "그럴듯한 수식을 먼저 세우고 소스를 나중에 확인해서" 생긴 오류다.
앞의 넷은 사용자가 잡아냈고, 조이너 배율 한 건은 시트가 새 탭을 내면서 드러났다.

| 틀렸던 전제 | 실제 (`병종 스탯 시스템 가이드`) |
|---|---|
| "궁병만이 상대 보병을 지운다 / 창병이 궁병을 일방적으로 잡는다" | 상성은 **T1 스킬의 공격 +10% 보너스**다. 모든 병종이 모든 병종을 때린다. **궁병 없어도 보병을 잡는다.** 창병의 후열 침투는 **T7 Ambusher, 20% 확률** |
| "동시 랠리 N개면 화력이 N배로 합쳐지고 상대 창병이 `L/N`로 분산된다" | **각 랠리가 별개 전투로 순차 처리**된다. 1번 랠리가 개리슨 전체와 싸우고 살아남은 개리슨이 2번 랠리와 싸운다. **랠리 수를 늘려도 한 전투의 판정은 그대로다** |
| "조이너 넷의 배율은 각자의 한계 배율을 곱하면 된다" | **같은 칸에 겹치면 합연산이라 포화한다.** 시트 `Rally Joiners` 탭이 4×제시를 `1.25⁴=2.441` 이 아니라 **2.0** 으로 적었다. 넷을 칸에 다 넣고 최종 칸으로 재야 한다 (2026-09-01 수정) |
| "병종 한정 스킬은 칸(SkillMod)과 다른 자리다" | **원문이 "Damage Dealt" 면 같은 스탯이다.** 플린트 `Pyromaniac` 은 "increases his Infantry's **Damage Dealt**" 라 제시의 "damage dealt" 와 같은 칸이다. 칸을 우회하니 그 스킬만 1.00 에서 재어져, 리더 A칸이 1.60 인데도 조이너 플린트가 ×1.310 으로 3위에 올랐다. `bk` 로 칸에 귀속시켜 해결 (2026-09-01, 사용자가 잡음) |
| "조이너 넷을 칸에 넣을 때 `bk` 와 `pc` 는 알아서 들어간다" | **아니다. `comboAll()` 에 버그가 둘 있었다.** ① `bk` 가 붙은 X 조이너(노라)를 칸에 넣지 않고 **리더 기준 한계 배율을 그냥 곱했다** → 제시·제셀이 같은 A칸에 들어가는데 겹침을 놓쳐 **과대**계상(×2.706 vs 정확 ×2.596). ② `pc` 조이너(미아)를 `e.v`=0.25 로 더했다 → 3병종 기대값 0.4375 여야 하므로 **과소**계상(×2.257). 부호가 반대라 서로 일부 상쇄되며 화면에 ×2.353 이 찍혀 있었다. **둘 다 "추천에 그 조합이 나온 적이 없어서" 안 드러났고, 시트 등재만을 기본으로 바꾸자 한 화면에 같이 떴다.** 검사도 같은 실수를 복제하고 있었다(`base`·`b` 가 `e.v` 를 썼다) (2026-09-12) |
| "병종 한정이 아니면 '추가피해'도 피해량 칸이다" | **아니다. 원문이 `extra damage / extra attack / N% damage` 면 Damage Dealt 스탯이 아니다.** X 스킬에는 이미 "extra damage 계열에 `bk` 를 달지 말 것"이라고 적어 놓고, **일반 칸 쪽은 똑같은 계열 8건을 A칸에 합산**하고 있었다 — 규칙이 한쪽에만 적용돼 있었다. 그래서 제로니모+브레들리 리더에서 A칸이 **2.05** 까지 부풀고, `bk` 가 붙은 노라가 ×1.197(10위)로 밀려 시트 1위(노라×3)와 어긋났다. `slot:"AH"` 로 분리해 칸 없이 곱하게 고쳤다 → A 1.80, 노라 7위, 시트 #1 재현 **80.8% → 84.6%** (2026-09-12) |
| "미아의 50% 확률은 한 번 굴린다 → 기대값 25%" | **병종마다 독립으로 굴린다.** 원문이 "Grants **all troops' attack** a 50% chance". 3병종이면 발동 `1−0.5³ = 87.5%`, 기대값 **43.75%** 로 25% 조이너의 1.75배다. `pc` 필드로 편성 병종 수를 반영 (2026-09-02) |
| "표의 🚫 열에 없으면 통과" | 표는 **금지 목록이지 승인 목록이 아니다.** 침묵을 통과로 렌더링하면 안 된다 → 판정을 4상태로 분리(`ban`/`counter`/`silent`/`noRow`) |
| "근거가 약해도 단서를 달아 두면 판정을 내놔도 된다" | 아니다. 임계값 셋이 각각 밴드 한 건에 묶인 지표에 "믿지 마세요"를 붙여 내보내고 있었다. **그 정도 단서가 필요하면 그 판정은 안 내는 게 맞다** → 3채널 지표 제거(2026-08-22) |

**규칙: 모델이나 수식을 세우기 전에 반드시 원본 가이드에서 실측치를 먼저 확인한다.
근거가 없으면 "자료 없음"이라고 밝히고 넘어간다. 추측을 메커니즘처럼 서술하지 않는다.**

시트가 **멀티랠리를 전제한 카운터**가 둘 있다(60/40·60/30/10 행의 `40/20/40`). 표가 추천이라고
적었으니 추천으로 표시하되, 라벨의 "+ 멀티랠리 3~4개" 를 지우지 말 것 — 단일 랠리로 된다는 뜻이 아니다.
(예전에는 이 두 건이 자체 지표와 엇갈렸고 그 불일치를 설명하려다 두 번 철회했다. 지표를 없애면서
불일치 자체가 사라졌지만, **설명을 지어내지 않는다**는 규칙은 그대로다.)

### 4. 2자리 비율 표기

3자리는 항상 **보/창/궁**. 2자리 약칭은 빠진 병종이 0이라 매번 다른 자리를 가리킨다.

`60/40` = 보60·창40 · `70/30` = 보70·창30 · `50/50` = 보50·궁50 · `49/49` = 보49·창2·궁49 · `40/60` = 보40·궁60

예외 하나 — "40/60 초방어를 깨는 50/50"만 **보50·창50**(창병으로 상대 궁60 저격이 목적).
사용자가 이걸로 입력 실수를 한 적이 있어 UI에 프리셋 칩과 합계 경고를 넣었다. **제거하지 말 것.**

---

## 구조

```
build.mjs          src/* 를 순서대로 이어붙여 index.html 생성 (번들러 없음, 순수 연결)
.gitattributes     작업 트리를 LF 로 고정 — 안 그러면 core.autocrlf 가 산출물 바이트를 바꿔 --check 가 깨진다
.gitignore         .omc/ (OMC 세션 상태) · node_modules/ · package.json · package-lock.json
                   ※ index.html 은 빌드 산출물이지만 Pages 가 서빙하므로 무시하지 않는다
index.html         빌드 산출물. 직접 편집 금지 — 항상 src 를 고치고 다시 빌드
img/heroes/*.webp  영웅 초상 61장 (128px, 장당 ~4KB, 합계 233KB). 파일명이 곧 영웅 id 다
img/ids.json       영웅 id → wosheroes 게임 ID (스킬 데이터 재수집용 참고)
src/
  part1.html       마크업 + CSS + <script> 여는 태그 · 탭 바 · #tab-comp / #tab-gear
  i18n.js          LANG 결정, L(ko,en), HN(hero), STR(정적 마크업 대응표), i18nFill
  data.js          영웅 61명 데이터베이스 (에픽 9 + Gen 1~17)
  gear-data.js     홍색 장비 상수 — 마일스톤·슬롯·병종·마스터리·XP 구간·경고 (L() 을 쓰므로 i18n 뒤)
  engine.js        편성 계산 로직 + calc() (DOM 읽기)
  gear-engine.js   장비 계산 — 순수 함수만. DOM 접근도 상수 하드코딩도 없다
  render.js        편성 출력 HTML + 프리셋/합계 경고 + init IIFE
  gear-render.js   장비 탭 렌더 + 탭 전환 + JSON 입출력 + 닫는 script/body/html 태그
test/
  fixtures.mjs     가이드 카운터표 — 편성 쪽 정답지 (랠리 11점 + 그것을 뒤집은 수비 11점)
  dom.mjs          최소 DOM 스텁 — 브라우저 없이 render.js·gear-render.js 까지 돌려 결과 HTML 을 받는다
                   PARTS 는 build.mjs 에서 끌어오므로 파트가 늘면 자동으로 따라간다
  unit.mjs         편성: 빌드 최신 · 스키마 · 카운터표 무결성 · 랠리/수비 판정 · 렌더 · i18n
  gear-unit.mjs    장비: 청크·마스터리·순서 28스텝·무료 XP·엣지 케이스 골든 픽스처 (정답지가 이 파일 안에 있다)
  browser.mjs      Playwright 로 랠리·수비 4상태 · 프리셋 · 장비 탭 · 딥링크 · 한글 잔존 · pageerror 검사
```

**조립 순서가 곧 의존 순서다:
`part1 → i18n → data → gear-data → engine → gear-engine → render → gear-render`.**
`i18n` 이 앞인 이유 — `engine.js` 도 `gear-data.js` 도 최상위에서 `L()` 을 호출한다.
`gear-render` 가 맨 뒤인 이유 — 닫는 `script`·`body`·`html` 태그를 마지막 파트가 낸다.
※ **주석 안이라도 닫는 script 태그를 문자 그대로 쓰지 말 것.** HTML 파서는 주석을 모르고
거기서 스크립트를 끊는다 — 실제로 그렇게 한 번 페이지가 통째로 죽었다(테스트가 잡았다).
`build.mjs` 는 배너 한 줄도 넣지 않는다. 순수 연결이라야 "분해 전후 바이트 동일"이 검증 수단으로 남는다.

단일 파일을 고집하는 이유: 서버 없이 파일로 열려야 하고, GitHub Pages에 그대로 올라가야 하며,
**블로그 CMS가 `<script>`를 제거**해서 계산기를 블로그 본문에 넣을 수 없기 때문(외부 호스팅 + 링크가 유일한 방법).

### 명령

```bash
node build.mjs           # src → index.html
node build.mjs --check   # 산출물이 src 와 일치하는지만 확인 (쓰지 않음)
node test/unit.mjs       # 편성. 빠름. 커밋 전 항상
node test/gear-unit.mjs  # 장비. 빠름. 커밋 전 항상
node test/browser.mjs    # Playwright 필요. 없으면 실패가 아니라 건너뛴다(exit 0)
```

`test/browser.mjs` 는 리눅스 CI에서 `/opt/pw-browsers/chromium-*` 를 자동 탐색한다.
다른 환경이면 `PW_CHROME=/path/to/chrome node test/browser.mjs`.
Playwright 는 저장소 의존성이 아니다 — 필요할 때 `npm i -D playwright` 로 넣는다.

### 영웅 선택 UI — 초상 픽커

리더 3칸은 `<details class="pick">` 안에 그 병종 영웅을 초상 격자로 깔고, 눌러서 고른다.
**진짜 상태는 여전히 숨은 `<select id="hInf|hLan|hMar" hidden>` 가 들고 있다** —
`calc()` 도 테스트도 그 `value` 만 읽으므로 판정 로직은 픽커를 전혀 모른다.
픽커는 값을 넣고 `change` 이벤트를 쏘는 화면 계층일 뿐이다. 이 분리를 깨지 말 것.

`<details>` 를 쓰는 이유는 열고 닫기·키보드 포커스·모바일 동작을 브라우저가 해주기 때문이다.
직접 만든 드롭다운으로 바꾸면 그걸 전부 다시 짜야 한다.

`rebuildSelects()` 가 픽커도 다시 만든다(언어가 바뀌면 이름이 바뀌므로).
격자 첫 칸은 **선택 해제**(`data-id=""`) 버튼이다 — 리더를 비우고 계산할 수 있어야 한다.

### 영웅 초상

`img/heroes/<영웅 id>.webp` 를 상대 경로로 부른다. `data.js` 에 URL 을 두지 않는다 —
**파일명이 곧 매핑**이라 영웅을 추가하면 같은 이름의 파일만 넣으면 된다.
못 불러오면 `onerror="this.remove()"` 로 조용히 사라진다. 그림이 없어도 계산은 그대로다.

다시 받아야 할 때(새 세대 추가 등):

1. 슬러그는 `en` 을 소문자화하고 공백을 `-` 로 바꾼 것이다 (`Seo-yoon`→`seo-yoon`,
   `Lumak Bokan`→`lumak-bokan`). 45명 전원 이 규칙으로 맞는다.
2. `https://wosheroes.com/heroes/<슬러그>` 에서 **`class="hero-portrait-img"` 가 붙은
   `hero_fullpic_<게임ID>`** 를 집는다. 페이지 첫 이미지를 그냥 긁으면 관련 영웅 것이
   섞인다 — 실제로 한 번 그렇게 잘못 받았다.
3. 초상은 `https://wosheroes.com/icons/hero_headpic_<게임ID>.png` (128px PNG, ~33KB).
   96px webp(q82)로 줄이면 장당 4KB 다.
4. 받은 뒤 원본과 대조할 때는 **알파를 같은 배경에 합성한 뒤** 비교할 것.
   `convert("RGB")` 로 알파를 버리면 투명 영역 때문에 멀쩡한 그림도 다르게 나온다.

`unit.mjs` 가 45장 존재·잉여 파일 없음·총용량·렌더된 경로가 실제 파일과 맞는지 검사한다.

### 테스트가 결함을 다루는 방식

`unit.mjs` / `browser.mjs` 상단의 **`KNOWN_DEFECTS`** 는 아직 안 고친 결함 목록이다.
여기 이름이 올라 있으면 실패로 세지 않고 `🔧 알려진 결함` 으로 찍는다. 고치면 목록에서 지운다 —
목록에 있는데 통과하면 `✨ 고쳐진 것 같습니다` 로 알려주므로 지우는 걸 잊지 않는다.
**새 결함을 발견했다고 KNOWN_DEFECTS 에 넣지 말 것.** 이건 이미 파악·기록된 것만 담는 자리다.

`dom.mjs` 는 `getElementById` · `value` · `innerHTML` · `classList` 정도만 흉내내는 스텁이다.
덕분에 `unit.mjs` 가 **렌더 결과 HTML 과 문단 순서**까지 본다 — 문자열이 있는지만 보면
"아래 설명문은…" 이 가리키는 문단이 위에 있어도 통과해버린다(실제로 그렇게 배포된 적 있다).
레이아웃·CSS·클릭 전파는 스텁으로 못 보므로 그건 `browser.mjs` 몫이다.

`SHEET_ROWS`(52행)는 **정답지가 아니라 대조표**다. 시트 세대별 행의 리더·병비·조이너를 옮겨 두고,
우리 조이너 순위가 시트에서 얼마나 멀어졌는지만 **하한선**으로 감시한다
(#1 재현 ≥88% · #1~#4 겹침 ≥58%, 실측 **92.3% / 61.4%**).
측정은 **⑤ 기본 추천 패널(`#rec` = 시트 등재만)** 을 읽고, **`SHEET_EDGE=0` 으로 시트 우선을 끔 채** 재다 —
켜 두면 순환논이다(아래 「애매하면 시트를 따른다」 참고).
100% 를 목표로 삼지 말 것 — 위에 적은 대로 시트는 최적해가 아니다.

2026-09-12 에 이 대조를 두 군데 고쳤다. **둘 다 검사 쪽 결함이었지 엔진이 아니었다:**

1. **⑤ 순위표를 긁고 있었다.** 순위표는 `x.dup`(리더 중복) 행을 그대로 싣는데 실제 추천은 그걸 빼고
   고른다. 그래서 미아가 리더인 행에서 "우리 1위 = 미아"로 세어져 재현율이 낮게 찍혔다 —
   같은 엔진인데 **73.5% → 81.5%**. 지금은 추천 패널(`id="rec"`)을 읽는다. 화면에서 사람이 읽는 것도 거기다.
   **순위표를 긁는 코드로 되돌리지 말 것.**
2. **행이 49개뿐이었다.** 게시본 텍스트를 긁다가 Gen 6 두 행(헥터·노라·웨인 45/15/40 · 헥터·몰리·웨인
   50/0/50)을 놓쳤다. CSV 로 다시 받아 54행 → 중복 2건 제거 → **52행**이다.

그리고 `pri`(조이너 #1~#4)와 `all`(+ Alternative 칸)을 나눠 뒀다. **하한선은 `pri` 로 잰다** —
`all` 은 시트가 "또는 이것도" 라고 적은 칸이라 넷만 고르는 우리 추천과 분모가 맞지 않는다.

`fixtures.mjs` 는 성격이 다르다. 결함 목록이 아니라 **가이드 표를 그대로 옮긴 정답지**다.
`SHEET_POINTS` 11점(판정이 `counter`/`ban` 이어야 하는 자리)과 `QUIET_POINTS`(표가 침묵하는
자리 — `silent`/`noRow` 로 나와야 한다)로 나뉜다. 침묵을 통과로 렌더링하지 않는지가 후자의 요점이다.

---

## 장비 탭 (홍색 영웅장비)

편성 계산기와 **탭**으로 나뉜다. `?tab=gear` 로 딥링크되고 기본은 `comp`.
편성 쪽 마크업은 `#tab-comp` 로 감싸기만 했고 판정 로직은 손대지 않았다.

### 병종마다 쓰는 축이 다르다 — 두 번째 1차 자료

출처: 커뮤니티 **「HERO GEAR — UPGRADE ORDER」** 표(jiujitefu-jitsu, State 2062). 32칸 우선순위표다.
이 표의 **좌우 사이클 · 골드 스탯 16칸이 우리 `GEAR_MS`/`GEAR_SLOTS` 와 전부 일치**했다(교차검증 통과).
표가 새로 더해 준 것이 `GEAR_AXIS` 다:

| | 초록(필수) | 노랑(추천) | 회색(맨 마지막) |
|---|---|---|---|
| 보병 | Defense · Health | — | Attack · Lethality |
| 창병 | Attack · Lethality | — | Defense · Health |
| 궁병 | Attack · Lethality · Health | Defense | 없음 |

`GEAR_ORDER` 에는 그 표의 **빨간 동그라미 1~32** 를 그대로 옮겨 뒀고 ⑤ 에 찍는다.
구조가 규칙적이라 `gear-unit.mjs` 가 **복사 검사가 아니라 규칙 검증**을 한다:
1~32 가 겹치지도 빠지지도 않는가 · 단계마다 딱 8칸(GOLD 1\~8 · +20 9\~16 · +60 17\~24 · +100 25\~32)인가 ·
단계 안 순서가 **보병2 → 궁병2 → 창병2 → 궁병2** 인가 · 빨강 단계의 궁병 4칸이 필수2 → 추천2 인가 ·
GOLD 1\~6 이 주 스탯 조각이고 7\~8 이 궁병 체력인가 · 번호 붙은 칸의 가중이 전부 0보다 큰가.

⚠️ **1~8 은 전부 골드 장비 단계다.** 표는 "홍색보다 골드가 먼저"라고 말하고 있지만 이 계산기는
홍색만 계산한다 — ⑤ 에는 그대로 보여주되 ④ 순서표와 섞지 않는다.

**회색은 금지가 아니라 우선순위 맨 뒤다.** 표가 번호를 안 붙였을 뿐, +100 까지 가면 어차피 지난다.
그래서 값 0 으로 두되 **버리지 않고** 효율 0 짜리 "완성용" 구간으로 맨 뒤에 붙인다
(`gearPlan` 이 잘린 청크 뒤에 `leftover` 를 단다). 총 미스릴·에센스·신화는 역할 가중을 켜도 꺼도 같고,
**바뀌는 건 순서와 "실제 쓸모"뿐**이다.

⚠️ **초록 1.0 · 회색 0 은 표를 그대로 읽은 값이지만, 노랑 `GEAR_AXIS_SUB = 0.75` 는 우리가 고른 값이다.**
값을 쓸어 보면 순서가 **평탄 구간**으로 갈린다 — `0.50~0.60` / `0.70~0.85` / `1.00`. 0.75 는 가운데 구간의
가운데라 ±0.07 흔들려도 순서가 안 바뀐다. `1.00` 은 표가 궁병 방어만 노랑으로 따로 칠한 구분을
지워 버리므로 쓰지 않는다. 숫자를 바꿀 일이 있으면 **평탄 구간을 먼저 다시 재고** 고를 것.
화면 라벨은 **필수 / 추천 / 맨 마지막** 이다(표 원문 키는 Recommended / Secondary / Not important).
표는 "Secondary" 라고만 적었지 숫자를 안 준다. 화면 ⑥ 한계에 그렇게 밝힌다.

### 왜 "청크" 로 자르는가 — 이 설계의 핵심

능력부여 마일스톤은 다섯이다. **원정**(Lv.20·60·100)만 랠리·성 전투·개리슨에 적용되고,
**탐험**(Lv.40·80)은 아레나 전용이라 랠리 리더에겐 기여가 **0**이다. 그런데 건너뛸 수 없다 →
탐험은 비용이지 이득이 아니다. 그래서 **통행료**라고 부른다.

청크는 **통행료 여럿 + 값 있는 마일스톤 하나**로 자른다(`gearCut`). 값 0 짜리 마일스톤은
그다음 값 있는 마일스톤에 붙는다 — 탐험이든 "이 병종이 안 쓰는 축"이든 취급이 같다.
그래서 "Lv.40 에서 멈춤"은 선택지가 아니다(아레나 가중을 켜면 Lv.40 에도 값이 붙어 정차역이 된다).

```
값 = (필요 축 가중 × 원정 보너스)  또는  (아레나 가중 × 탐험 보너스)
eff = 구간 값 합 / 구간 미스릴 합
```

**모든 축 가중을 1로 두면 이 자르기는 예전의 "탐험 + 다음 원정" 과 정확히 같아진다** —
`gear-unit.mjs` 가 그걸 검사한다(28스텝 골든이 `FIX_FLAT` 으로 그대로 통과). 새 모델이 옛 모델을
포함한다는 증명이라 이 검사를 지우지 말 것.

⚠️ **값 있는 마일스톤끼리는 합치지 않는다 — 예전에 합쳤다가 철회했다.**
2026-08 까지는 누적 (미스릴,값) 점들의 **위쪽 오목 껍질**(`gearHull`)로 잘랐다. 껍질이면 조각 안에서
효율이 단조 감소해서 그리디가 깔끔했지만, 값이 낮은 마일스톤이 뒤의 높은 것에 흡수되면서
**한 조각을 +20 에서 +100 까지 140 미스릴로 한 번에 올리는** 청크가 생겼다(궁병 좌 계열).
순서표를 위에서부터 따라가는 사람에게는 그 자리에서 미스릴이 통째로 묶인다 —
**예산 300 에서 최적 270 대비 210** 밖에 못 냈다. 사용자가 "하나를 계속 올리는 식"이라고 잡았다.
껍질을 버리니 그 예산에서 270(최적)이 나오고, 정차역이 살아나서 `gearBudgetPick` 의 DP 도
**모든 마일스톤 정차역**을 후보로 갖게 됐다(예산 400 에서 312.5 → 315, 이제 진짜 최적).

대가는 **조각 안에서 효율이 단조가 아니라는 것**이다. 궁병 고글은 Lv.60(방어 0.75 · eff .45)이
Lv.100(공격 1.0 · eff .556)보다 낮다. 그래서 순서표에서 효율이 한 번 도로 올라간다 —
60 을 지나야 100 에 닿으므로 순서 자체는 옳다. **효율 열이 단조라고 가정하는 코드를 쓰지 말 것.**

### 정렬 — 단계가 1순위, 그 안에서 효율 내림차순 + 예산은 따로 푼다

**1순위는 효율이 아니라 단계다** — 12조각의 +20 을 전부, 그다음 +60 을 전부, 그다음 +100
(`chunk.stage` = 이 청크가 여는 원정 마일스톤의 순번). 효율만으로 정렬하면 위의 비단조 때문에
궁병 좌 계열이 혼자 +100 까지 올라가고 나머지가 +20 에 머문다. 근거는 둘이다:
**원본 우선순위표가 단계별로 돈다**(9~16 = +20 · 17~24 = +60 · 25~32 = +100), 그리고
**실측으로 임의 예산 손실이 42.5 → 40%p 로 오히려 줄었다.** 효율 정리에서 유도한 게 아니다 —
표와 실측이 근거다. 화면 ④ 에 `tr.gband` 로 단계 띠를 찍어 이 사실을 드러낸다.

⚠️ **아레나 정차역(탐험)은 이 단계 밖이다.** 단계에 끼우면 실전 기여 0 인 Lv.40 을 다른 조각의
Lv.60 보다 먼저 사게 된다. 원정 단계 전부 뒤, 완성용 앞에 놓는다.

⚠️ **"모든 예산 지점에서 최적"인 순서는 존재하지 않는다.** 예전에 화면과 이 문서에 효율
내림차순이 그렇다고 적어 놨는데 사실이 아니었다. 다음 단계가 커서 안 들어가면 미스릴이 논다 —
어떤 고정 순서를 써도 그렇다(배낭 문제의 끼워맞춤 효과). 지금 순서의 최대 손실은 정확해 대비
40%p, 원본 표 대비 25%p 이고 `gear-unit.mjs` 가 그 상한을 지킨다.

그래서 **예산 슬라이더는 목록을 자르지 않고 `gearBudgetPick()` 으로 정확히 푼다.** 조각마다
"앞에서 몇 청크까지"만 고르면 되므로(청크는 순서를 못 건너뛴다) 다중선택 배낭이고, 미스릴이
전부 10 단위라 DP 로 최적해가 나온다. 그래서 예산을 줄였을 때 뜨는 목록은 **순서표의 앞부분이
아닐 수 있다** — 번호가 건너뛴다. 화면에 그렇게 적어 뒀다.

`gear-unit.mjs` 가 10 단위 전 예산에서 **우리 해가 원본 표 순서보다 못한 곳이 없는지**,
**순서대로 자르기보다 못한 곳이 없는지**, 그리고 **④ 를 위에서부터 그대로 따라갔을 때
원본 표보다 25%p 넘게 뒤지는 예산이 없는지**를 훑는다. 예산 300 은 두 결함을 다 잡은
자리라 회귀 표식으로 따로 박아 뒀다(정확해 270 · ④ 프리픽스도 270).

동률 타이브레이크(단계 → 효율 다음): ① 병종 가중(`공격가중×공격비율 + (1−공격가중)×수비비율`) →
② 주 스탯 여부(슬롯 stat === 병종 mainStat) → ③ 좌우 방향(딜러는 attack, 탱커는 defense) →
④ `GEAR_TROOP_ORDER`/`GEAR_SLOT_ORDER`/seq 고정 순서.
구현은 조각마다 포인터를 두고 매번 후보 중 최선을 뽑는 방식이라 **seq 순서가 자동으로 지켜진다**.

### 정답지는 `test/gear-unit.mjs` 안에 있다 — 둘이다

1. **역할 가중 OFF (`FIX_FLAT`)** — 28스텝 전체 순서·티어·무료 XP·총계. 예전 모델 그대로다.
2. **원본 우선순위표 대조** — 그림을 손으로 옮긴 `CELL`/`NUM` 표와 맞춘다. 좌우 사이클 16칸,
   `GEAR_AXIS` 36칸, 그리고 **백지에서 계획을 세웠을 때 값이 붙는 마일스톤이 표의 번호 붙은
   빨강 24칸과 정확히 같은가**. 마지막 것이 가장 강한 검사다 — 우리 출력에서 뽑은 게 아니라
   그림에서 유도했다.

편성 쪽 `fixtures.mjs` 와 같은 성격이다 — 28스텝 전체 순서·티어 4개·무료 XP 10건·총계
(미스릴 1560 · 원정 +1020%p · 에센스 4400 · 신화 120)·엣지 케이스 6건이 박혀 있다.
**어긋나면 엔진이 틀린 것이지 픽스처가 틀린 게 아니다.** 이 도메인은 직관이 반복해서 틀리는 곳이라
수치를 먼저 고정하고 엔진을 맞췄다.

### 12조각 입력 — 카드 그리드

**병종 3칸(보 · 창 · 궁)이 가로로 서고, 한 칸 안에서 고글·장갑 / 벨트·신발 두 줄**이다.
좌 계열(고글·벨트)이 왼쪽 열, 우 계열(장갑·신발)이 오른쪽 열로 세로로 맞는다 — 좌우가 공/방
배분만 다른 같은 총량이라, 그 대비가 자리로 보이는 편이 낫다.
카드마다 슬롯 아이콘(인라인 SVG · 외부 파일 없음) · 좌/우 배지 · `M`/`+` 입력 · **마일스톤 트랙**이 들어간다.
`.gwrap` 이 `auto-fit minmax(322px,1fr)` 이라 좁아지면 3 → 2 → 1칸으로 접히고, 병종 안의 2×2 는 유지된다.

조각 이름은 게임 표기를 따른다 — **고글 / Goggles**(헬멧이 아니다). 다만 **내부 키는 `helmet` 그대로**다:
`GEAR_ORDER`·`GEAR_SLOTS`·입력칸 id(`gm_<병종>_helmet`)·픽스처가 전부 이 키를 쓰므로 바꾸면 골든이 통째로 흔들린다.
보이는 이름과 키는 별개다 — `GEAR_SLOTS.helmet.ko/en` 만 게임 표기다.

**행 순서는 `GEAR_ROW_ORDER`(화면 전용)를 쓰고 `GEAR_TROOP_ORDER` 를 쓰지 않는다.**
후자는 엔진의 마지막 타이브레이크라 바꾸면 골든 픽스처의 28스텝 순서가 흔들린다.
보이는 순서와 정렬 기준은 별개다 — `gear-unit.mjs` 가 둘 다 검사한다.

마일스톤 트랙이 이 화면의 핵심이다. 0~100 위에 눈금 다섯을 찍고 **원정(20·60·100)은 `--star`,
탐험(40·80)은 흐린 회색**으로 갈라 놓는다. "탐험은 미스릴만 내고 실전 기여가 0"이라는 이 도구의
가장 중요한 사실을 표로 설명하는 것보다 눈금 색 하나가 빠르다. 지나온 눈금은 흐려진다.

**트랙은 `gearTracks()` 가 자식 노드(`gt_<병종>_<슬롯>`)에만 다시 쓴다.** 카드를 통째로 다시
만들면 타이핑 중인 칸의 포커스가 날아간다. `browser.mjs` 가 포커스 유지를 검사한다.
(`dom.mjs` 스텁은 진짜 트리가 아니라 자식 변경이 부모 `innerHTML` 문자열에 안 비친다 —
트랙을 검사할 때는 부모를 훑지 말고 그 노드를 직접 읽을 것.)

주 스탯 카드(슬롯 stat === 병종 mainStat)는 아이콘이 `--accent` 로 켜진다 — 보병은 장갑·벨트,
딜러는 고글·신발 여섯 장이다.

### 규칙

- **`gear-engine.js` 에 숫자를 하드코딩하지 않는다.** 상수는 전부 `gear-data.js` 에 있고,
  `gear-unit.mjs` 가 `0`·`1`·`100`(항등원과 % 환산) 말고 다른 숫자가 있으면 실패시킨다.
- **`gear-engine.js` 는 DOM 을 만지지 않는다.** 입력은 `gear-render.js` 가 읽어서 넘긴다.
- **M·+ 두 칸이 다 비면 그 조각은 계획에서 뺀다.** `gearInput()` 이 항목 자체를 안 만들고
  `gearPlan()` 이 없는 항목을 건너뛴다. 0 으로 읽으면 **안 갖고 있는 조각까지 처음부터 올리는
  계획**이 나와서 총액이 통째로 거짓말이 된다 — 첫 화면에 36스텝·미스릴 1800 이 떠 있던 걸
  Orca 브라우저에서 잡았다. 하나도 입력 안 했으면 계획 대신 **안내**를 내고 ⑤·⑥만 남긴다.
- **마스터리는 돌파(마일스톤)에만 걸린다.** 마일스톤 사이 레벨업은 XP 만 드므로, `gearLevelCap` 의
  상한은 "마지막으로 통과한 마일스톤"이 아니라 **못 여는 첫 마일스톤 −1** 이다 —
  M11 은 Lv.20 을 뚫을 수 있고 Lv.40 은 못 하므로 **+39** 까지다(+20 이 아니다).
  예전에 +20 으로 잡아 게임에서 멀쩡한 M11/+39 에 경고가 떴다(사용자가 잡음).
- 마스터리 상한을 넘는 입력은 **경고만 하고 계산은 진행**한다 — 우리 표보다 게임 쪽이 1차 자료다.
- **병비는 정규화하지 않는다.** 순서 타이브레이크 공식이 `공격비율/100`·`수비비율/100` 을 그대로
  쓰기 때문에, 공격·수비 두 줄의 합이 서로 다르면 두 축 무게 비율이 실제로 틀어진다 →
  `gearSums()` 가 합이 100이 아닐 때 그 사실을 그대로 적는다(편성 탭처럼 "정규화합니다"라고
  쓰면 거짓말이다).
- **넓은 표는 `.xscroll` 로 감싸고 칸은 `nowrap` 이다.** 순서표는 열이 열 개라 390px 화면에서
  본문이 통째로 가로 스크롤됐다. 표만 자기 컨테이너 안에서 스크롤시킨다.
  그리고 **`#tab-gear table th,td` 는 `white-space:nowrap`** 이다 — 긴 설명이 들어가는 `작업` 칸이
  폭을 다 먹어서 조각 이름("보병 고글")이 1400px 화면에서도 공백에서 두 줄로 접혔다(배포본에서 잡힘).
  **접혀도 되는 건 `#gSteps td.work`(+그 안의 `.note`)와 `.cap` 뿐이다.** 예외를 `.note` 전체로 주면
  좌우 칸의 "방어"가 24px 안에서 **방/어 로 쪼개진다** — 한글은 음절 사이에서 끊긴다(실제로 그렇게 됐다).
- `.gwrap` 의 `minmax` 는 **`min(322px,100%)`** 이다. 322px 를 그대로 쓰면 320px 화면에서
  칸이 뷰포트보다 커져 본문이 넘친다.
- `browser.mjs` 가 1400~320px 여섯 폭에서 접힘과 본문 넘침을 검사한다.
  **검사 단위를 세 번 틀렸으니 그대로 두자:**
  ① 칸 높이로 보면 **패딩을 접힘으로 오인**한다.
  ② 칸 단위 `Range` 는 뱃지가 섞이면 `getClientRects()` 가 여러 개라 **멀쩡한 칸이 전부 두 줄로** 잡힌다.
  ③ 자식이 있는 칸을 빼면 **좌우 칸(`<div class="note">`)을 놓친다** — 실제로 놓쳤고 사용자가 잡았다.
  → **표 안의 텍스트 노드마다** `Range` 로 줄 수를 센다. 일부러 접히는 `.work`/`.cap` 만 제외한다.
  범위를 표 밖까지 넓히면 슬라이더 안내문 같은 정상 문단이 전부 걸린다.
- 그리드 입력과 체크박스에는 **`aria-label`** 을 붙인다. placeholder 는 접근명이 되지 않는다.
- `+1` 이 미돌파라는 안내를 그리드 아래에 **상시** 노출한다. 이 오해가 비용을 두 배로 틀리게 한다.
- 경고(`GEAR_WARNINGS`)와 한계(`GEAR_CAVEATS`)는 **접지 않는다.** 브라우저 검사가 `details` 개수 0을 본다.
- **예산 슬라이더 값을 총액으로 덮어쓰지 않는다.** `GEAR_BUDGET` 은 사용자가 실제로 움직였을 때만
  값이 들어가고, 표시할 때만 총액으로 클램프한다. 덮어쓰면 조각 레벨을 잠깐 올렸다 내리는 사이
  총액이 줄었을 때 예산이 그 값에 눌러앉아 총액이 회복돼도 스텝이 계속 감춰진다(Orca 브라우저에서 잡혔다).
- 입력칸은 `input` 만 듣는다. `change` 까지 듣게 하면 포커스가 빠지는 순간 표가 다시 그려져서
  **"숫자를 고치고 바로 체크박스를 누르는" 첫 클릭이 삼켜진다**(실제로 그렇게 잡혔다).
  범위를 벗어난 값 되쓰기만 `change` 에서 하고, 값이 안 바뀌면 다시 그리지 않는다.
- `GEAR_CAVEATS`·`GEAR_WARNINGS` 는 `i18nFill()` 로 감쌌다. 첨부 원본은 최상위 `const` 였는데
  그러면 로드 시점 언어로 굳는다(작업 규칙 3).

---

## 데이터 스키마 (`src/data.js`)

```js
{id:"mia", s:1, kr:"미아", en:"Mia", cls:"lancer", gen:3, rar:"leg",
 exp:[{n:"Bad Luck Streak", slot:"G", v:.25,
       p:"50%", pe:"50% chance",              // 발동 조건 (한/영)
       t:"50% 확률 적 받는 피해 +50%",          // 설명 (한)
       te:"50% chance: enemy damage taken +50%"}],
 w:{side:"rally", stat:"Attack", name:"Rally of Fate"}}   // 전용무기 원정스킬
```

**영웅은 61명이다** — 에픽 9 + Gen 1~17 각 3명(병종당 1명). 2026-08-22 에 Gen 14~17 열둘과
에픽 둘(지나·료우키), Gen 13 둘(플로라·기젤라)을 보강했다. Rare 4명(Charlie·Cloris·Eugene·Smith)은
원정스킬이 채굴·벌목뿐이라 **일부러 뺐다** — 리더도 조이너도 못 되는 영웅이다.

데이터 출처는 <https://wosheroes.com/heroes/<슬러그>> 다(슬러그 = `en` 을 소문자화 + 공백→`-`).
페이지의 **Expedition Skills**(`Expedition` / `Expedition+`)와 **Exclusive Expedition**(전용무기
원정스킬 — 여기서 `side`/`stat` 이 나온다)을 읽으면 스키마가 그대로 채워진다.
기존 45명을 이 방식으로 재수집해 대조했더니 스킬명 135개·무기 38개가 전부 일치했다
(flint 의 무기 스킬명 `Dragon's Breath`→`Dragonbreath` 한 건만 정정).

**한글명은 <https://www.whiteoutsurvival.wiki/ko/heroes/> 표기로 통일한다.** 영문 페이지와
한글 페이지의 카드 이미지 파일명이 같아서 짝지을 수 있다. 위키에 없는 넷(료우키·에이든·버사·
엘리노어)은 사용자가 정해준 이름이다.

- `s:1` = **Ton 시트의 조이너 명단**에 오른 영웅(19명). UI에서 `시트` 배지가 붙고 별도 목록으로도 보여준다.
  **리더로만 나오는 영웅은 붙이지 않는다** — 이 플래그를 쓰는 곳이 ⑤ 의 "시트 등재 영웅만"
  조이너 추천 하나뿐이라, 리더까지 넣으면 시트가 조이너로 추천한 적 없는 영웅이 그 목록에 섞인다.
  범례 두 줄(`*Jessie = Jessie, Jasser or Jeronimo` · `**Sergey = Bahiti, Lumak Bokan`)을 펼친 결과이고,
  정답지는 `test/fixtures.mjs` 의 `SHEET_JOINERS` 다.
  2026-09-01 갱신(`Gen 10+` 탭 신설): 룸 보겐·료우키를 넣고, 리더로만 나오는 몰리·알론소·그렉을 뺐다.
  **조이너 명단은 여전히 Gen 8 이하뿐이다** — 새 탭이 늘린 것은 Gen 9~12 **리더**다.
- `rar:"epic"` 은 원정스킬 2개 + `w:null`. 데이터상 에픽은 **9명**이고, 그중 **지나만 `s` 가 없다**
  (나머지 8명 — 제시·제셀·서윤·패트릭·세르게이·바히티·룸 보겐·료우키 — 은 시트 조이너 명단에 있고,
  누구나 보유한다고 가정한다). 룸 보겐·료우키는 2026-09-01 시트 갱신에서 들어왔다.
- `slot` 코드 — 딜: `A` 피해량↑ `An` 일반공격한정 `B` 공격력↑ `E` 파괴력↑ `F` 적방어↓ `G` 적받는피해↑ `CRIT` /
  생존: `C` 체력↑ `D` 받는피해↓ `DEF` 방어력↑ `H` 적공격↓ `I` 적파괴력↓ `J` 적피해량↓ `DODGE` /
  특수: `X` 병종한정·주기형(`k:"dmg"|"sur"` 와 `tgt` 필수, 27명 보유) · **`AH` 타격 추가피해(칸 아님, 8건)** · `ECO` 비전투
- `also:{slot,v}` — 한 스킬이 두 칸을 채우는 경우. 일반 칸끼리(공격+방어 등)도 쓰고,
  **`slot:"X"` 에도 붙는다** — 한 스킬이 두 병종에 다르게 걸릴 때다
  (예: 플로라 `Plantage` = 창병 딜↑ + 보병 받피↓). 이때 `also` 도 `tgt`/`k` 를 갖는다.
  데미지 공식에서 두 효과는 서로 다른 자리에 꽂히므로(딜은 SkillMod, 받피는 생존 축)
  조이너 배율은 **두 지분을 각각 곱하고**, 리더 화면 ④에는 두 줄로 나온다
- **같은 칸은 합연산, 다른 칸은 곱연산.** 조이너 가치는 한계 배율 `(칸값 + v) / 칸값` 으로 계산한다 → 포화.
  시트 `Rally Joiners` 탭이 이 규칙을 숫자로 적어 뒀고, 그 탭의 무중복 4행이 우리 계산과
  소수점까지 일치한다(`test/fixtures.mjs` 의 `JOINER_POINTS`).
  **그 탭에는 "리더가 같은 스킬을 가진 경우" 블록 12행도 있다**(2026-09-12 수집, `JOINER_DUP_POINTS`).
  리더 기준값을 1.25/1.25 · 1.25/1.50 · 1.00/1.50 로 바꿔 가며 다시 계산해 둔 표라,
  **"리더가 채운 칸에 겹치면 깎인다"는 우리 모델의 주장을 시트가 직접 숫자로 적은 자리**다 — 12행 전부 일치한다.
  이 블록은 최적 배분이 **고정이 아니라는 것**도 보여준다: 제로니모 리더(A 1.25)에서는 2:2 가 4:0 보다 크고,
  마그누스+브레들리(A 1.00 · B 1.50)에서는 3:1 이 2:2 보다 크다.
  ⚠️ 이 픽스처는 **시트가 적은 기준값(a0·b0)을 그대로 먹인다.** 우리 리더 파싱을 쓰지 않는 이유는
  시트가 리더 원정스킬 3개 중 "납작한 25%" 둘만 세기 때문이다(제로니모의 `Expert Swordsmanship` 은 뺀다).
  그래서 이게 검증하는 것은 **칸 규칙과 한계 배율**이고 리더 데이터가 아니다.
- ⚠️ **조이너 넷을 합칠 때 각자의 한계 배율을 곱하지 말 것.** 셋 이상이 같은 칸에 들어가면
  포화를 통째로 놓친다. `render.js` 의 `comboDmg()` 처럼 **넷을 칸에 다 넣고 최종 칸으로 다시 잰다.**
- **`bk:"A"|"D"` — 병종 한정이지만 원문이 "스탯"인 X 스킬은 그 칸에 합산한다.**
  wosheroes 원문이 `Damage Dealt` / `Damage Taken` 을 올린다(내린다)고 적었으면 일반 칸과
  **같은 스탯**이다. 칸을 우회하면 그 스킬만 1.00 에서 재어져 혼자 포화를 안 겪는다 —
  리더 A칸이 1.60 인데 조이너 플린트가 `1 + 1.0×0.31 = 1.310` 으로 3위에 올라오던 게 그 증상이다.
  `bk` 가 붙으면 **리더는** `buck[bk] += v × 지분`, **조이너는** `(buck[bk] + v×지분)/buck[bk]` 로 잰다
  (지분은 딜이면 `dmgShare`, 생존이면 `tgtRatio/100`). 정답지는 `test/fixtures.mjs` 의 `X_BUCKETED` 10건이고,
  각 항목 옆에 원문을 인용해 뒀다.
- ⚠️ **`bk` 를 "추가피해(extra damage)" 계열에 달지 말 것.** `Nightmare Trace`(레니)·`Venom Infusion`(고든)
  처럼 타격에 붙는 것은 원문부터 `extra damage` 라 다른 기전이고, 어느 칸인지 **자료가 없다**.
  이것들은 여전히 `1 + v×지분` 이라 포화를 안 겪는다.
- **`slot:"AH"` — 위 규칙의 짝이다(2026-09-12).** 병종 한정이 아닌 일반 스킬도 원문이
  `extra damage / extra attack / N% damage` 면 Damage Dealt 스탯이 아니다 → **A칸에 합산하지 않고
  칸 없이 자기 계수로만 곱한다**(`ORDER` 에 일부러 넣지 않았다. 포화를 안 겪는다).
  해당 8건은 필리 `Dosage Boost` · 헥터 `Blitz` · 알론소 `Poison Harpoon` · 그웬 `Air Dominance` ·
  불카누스 `Breaker Steel` · 웨인 `Thunder Strike` · 브레들리 `Power Shot` · 카롤 `Shieldbreaker` 다.
  **판정 기준은 원문 한 줄뿐이다** — "피해량(damage)"을 올리면 A, "추가피해·추가공격·N% 피해"면 AH.
  그래서 미아 `Lucky Charm`("50% 확률 피해량 +50%")은 **확률형이지만 A 에 남는다.** 조건부라는 것이
  기준이 아니다(조건부 16건을 전부 빼면 시트 재현이 92.6% 까지 오르지만 **근거가 없어서 안 한다**).
  화면 ② 에 "🎯 타격 계열 — 칸이 아닙니다" 패널로 상시 노출한다.
- ⚠️ **레니 `Dreamcatcher` 는 스탯인데도 일부러 뺐다.** 원문은 "damage dealt to **marked targets**" 라
  분명 스탯이지만 `v` 가 표식 유지율을 환산하지 않은 원값(1.5)이다. 환산 안 된 값을 칸에 넣으면
  오히려 더 부풀려진다 → **환산부터 하고 붙일 것.**
- ⚠️ **X 포화 모델을 시트로 검증해 봤지만 시트가 못 가른다(2026-09-02).** 후보 넷을 시트 49행에
  먹여 재현율을 쟀다 — `none`(현행) · `class`(대상 병종별 공용 칸) · `one`(X 전체 한 칸) ·
  `toA`(전부 A칸에 접기):

  | 지표 | none | class | one | toA |
  |---|---|---|---|---|
  | 시트 조이너 집합 겹침(상위4) | 52.5% | 52.5% | 52.5% | **55.8%** |
  | 시트 **#1** 조이너 재현(상위4) | **76.9%** | 76.9% | 76.9% | 73.1% |
  | 제시 동치군 제외 겹침 | 58.0% | 58.0% | 58.0% | **61.6%** |

  **`none`·`class`·`one` 은 점수가 완전히 같고**(리더가 X칸을 채우는 일이 거의 없어 차이가 안 난다),
  `toA` 는 지표에 따라 이겼다 졌다 한다 → **노이즈다. 어느 모델도 지지되지 않는다.**
  게다가 `toA` 는 원문의 `extra damage` 를 `Damage Dealt` 스탯으로 취급하는 것이라 `bk` 규칙과 모순된다.
  **결론: X 포화는 손대지 않는다.** 상위6이 모델에 따라 달라지는 행이 23/52 나 되는데 점수가 안 움직이는
  것 자체가 "시트로는 이 질문에 답할 수 없다"는 뜻이다.

  ⚠️ **위 표의 숫자는 고치기 전의 측정이다.** ⑤ 순위표를 긁던 시절(리더 중복 포함)에 49행으로 쟀다.
  결론("어느 모델도 지지되지 않는다")은 네 모델이 **같은 측정에서 서로 비교된** 것이라 그대로 유효하지만,
  절대 수치를 지금 값(84.6%)과 나란히 놓고 읽지 말 것. 다시 판단할 일이 생기면 **추천 패널·52행·AH 적용
  상태에서 새로 쓸어야 한다.**

  시트가 못 가르는 이유도 분명하다 — **시트는 최적해가 아니라 보유 가능 목록이다.**
  10/52 행이 같은 영웅을 2~3장 겹쳐 쓰고(노라 9행) 이는 시트 자신의 지침 5번("4명을 섞는 게 훨씬 낫다")과
  어긋난다. 리더에 제로니모가 있어 A칸이 찬 행에서도 제시를 1순위로 적는다.
  가르려면 **실전 전투 리포트 같은 새 자료**가 필요하다.
- **노라는 "특수 케이스"라고 불리는데, 그 특수함은 이미 반영돼 있다(2026-09-12 확인).**
  볼트 「랠리 참여자 영웅 가이드」 257행이 *"48/4/48 에서 노라 3~4명 스택이 추천됨 — 노라의 스킬이
  스택 시에도 효율이 좋은 특수 케이스"* 라고 적고, 「랠리 편성·조이너·데미지 공식 통합 해설」 514행이
  기전을 준다: *"노라 1명이 받피 −15%(D)와 피해량 +15%(A)를 동시에 채운다 … 대상이 보병·궁병 한정인데
  48/4/48 은 보+궁이 96% — 페널티가 사실상 0"*.
  **그 기전이 곧 우리 `bk:"A"` + `also bk:"D"` 다.** 두 칸에 각각 지분 환산해 넣고 곱하므로,
  노라가 X 조이너 중 1위이고 48/4/48 추천에 자주 든다. **`bk` 를 떼야 할 근거가 아니다.**
  ⚠️ 다만 그 기전은 **노라 1명이 센 이유**를 설명할 뿐 **3명이 포화를 안 겪는 이유는 설명하지 않는다.**
  두 번째 노라는 같은 A·D 칸에 합연산으로 들어가므로 자기끼리 포화한다. 실제로 재 보면 시트가
  노라를 쌓는 8행 전부에서 **우리 추천이 3.4~16.9% 높다**(예: 로건·필리·웨인 45/5/50 추천 ×2.596 vs
  노라×3+패트릭 ×2.220). 그리고 시트 **자신의 지침 5번**이 "4명을 섞는 게 같은 영웅 4번보다 훨씬 낫다"다.
  → **시트의 "노라 ×3" 은 "여기서는 노라가 최고니 변주를 못 구하면 그냥 채워라"로 읽는 게 맞다**
  (지침 8번이 "캐주얼 연맹에서는 서로 다른 조이너 4명이 불가능할 수 있다"고 적어 둔 그 상황이다).
  이 대조는 `unit.mjs` 「전투 배율 — bk 겹침 · pc 기대값」 절이 회귀로 지킨다.
  뒤집으려면 **같은 편성에 노라를 1장/3장 넣은 실전 전투 리포트**가 필요하다.
  ⚠️ 단, **"둘째 노라"는 이야기가 다르다** — 2026-09-13 에 `stack:1` 로 열어 주자 순수 계산에서도
  3행이 노라 2장을 고르고 배율이 올랐다. 위 ×2.220 은 **3장을 넣은** 숫자다(아래 `stack:1` 항목 참고).
- **넷을 고르는 방법 = 포화를 보며 한 명씩(탐욕). 자르기가 아니다 (2026-09-12).**
  예전에는 ⑤ 순위 상위 4명을 그냥 잘랐다. 그러면 **같은 칸에 들어가는 영웅이 나란히 뽑힌다** —
  제시·제셀·제로니모는 전부 A칸이라 각자 ×1.250 이지만 둘째는 ×1.200, 셋째는 ×1.167 인데
  자르기는 그걸 모른다. 실제로 로건·필리·진먼 Gen 3 에서 **미아·제시·제셀·제로니모**가 나왔고
  사용자가 잡았다. 지금은 매번 `comboAll(고른것 + 후보)` 가 가장 커지는 한 명을 더한다.
  측정: 시트 52행에서 **자르기보다 낮아지는 행 0개**, 높아지는 행 22개(평균 +2.41%),
  #1 재현 **90.4 → 92.3%**, 겹침 **58.8 → 61.4%**. Gen 12 META 50/10/40 은 시트의 넷과 **정확히 일치**한다
  (미아·패트릭·헨드릭·노라). `unit.mjs` 가 "자르기보다 낮아지는 행 0" 을 회귀로 지킨다.
  동률은 pool 순서가 이기고, pool 은 이미 투자 문턱으로 정렬돼 있어 그 정책이 그대로 지켜진다.
- **중복은 `stack:1` 인 영웅에게만 허용한다 — 지금은 노라 하나뿐이다 (2026-09-13).**
  근거: 시트 세대 탭이 **10개 행에서 노라만 2~3장 겹쳐 쓰고, 다른 영웅은 한 번도 안 겹친다.**
  볼트 「랠리 참여자 영웅 가이드」 257행도 *"노라의 스킬이 스택 시에도 효율이 좋은 특수 케이스"* 라고 적었다.
  예전 노트가 "탐욕을 돌리면 같은 X 영웅 4명을 쌓는 답이 나온다"고 경고했는데, 그건
  **아무나 중복을 허용했을 때** 이야기다. 한 명만 열어 두면 시트 지침 5번과 어긋나지 않는다.
  ⚠️ **이전에 적어 둔 "노라 스택은 항상 손해"는 과장이었다(정정).** 그 측정은 **3장이 넷 중 셋을
  차지하는** 경우였고, **4번째 자리에 둘째 노라**는 다른 이야기다 — 중복을 허용하자
  **순수 계산(`SHEET_EDGE=0`)에서도 3행이 노라를 2장 고르고 배율이 올라갔다**
  (로건·필리·웨인 45/5/50 ×2.728 → **×2.742**). 시트 우선까지 켜면 5행이다.
  둘째 노라도 같은 A·D 칸에 합연산으로 들어가 **자기끼리 포화**하므로, **그래도 이득일 때만 뽑힌다** —
  "쌓아라"가 아니라 "고를 수 있다"는 플래그다.
  `unit.mjs` 가 둘을 같이 지킨다: **"노라 말고는 아무도 중복 안 된다"** · **"예외가 죽어 있지 않다(≥3행)"**.
  ⚠️ **3장은 2장과 근거가 다르다 — 여유폭으로 안 되고, 시트 칸을 콕 집어 따른다 (2026-09-13).**
  시트는 **6개 행에 노라 칸을 셋** 적어 뒀는데, 셋째 노라는 우리 계산으로 **−3.0~3.4%** 라
  2% 여유폭으로는 절대 안 잡힌다. 여유폭을 3.5% 로 넓히면 잡히지만 **무관한 행 8개가 같이 흔들리고**
  손실이 평균 0.36 → 0.98% 로 뛴다. 그래서 `pick4()` 가 **"시트가 같은 영웅을 또 요구하는 칸"만
  여유폭을 묻지 않고** 따른다. 단 **모델이 이미 그 영웅을 한 번은 뽑았을 때만** 늘린다 —
  시트만 보고 처음부터 밀어 넣지는 않는다.
  대가는 크다: **노라 3장 행에서 최대 −8.0%**(마그누스 48/4/48 추천 ×2.275 vs 순수 ×2.415, −5.8%).
  그래서 `unit.mjs` 의 손실 상한을 **둘로 갈랐다** — 여유폭 규칙은 ≤2.5%, 스택 강제는 ≤9%.
  **두 상한을 합치지 말 것.** 합치면 "여유폭이 지켜지는가"를 아무도 안 지키게 된다.
  ⚠️ 그래서 **`#recAll`(계산 순위 그대로)은 시트를 전혀 안 보게 바꿨다.** 예전에는 거기도 시트 우선을
  거쳐서 "시트 명단을 무시하면 이렇게 됩니다"라는 문구가 **거짓**이었다. 지금은 진짜 순수 계산이라,
  **시트를 따르느라 포기한 양이 화면에 그대로 보인다.** 노라 3장 행에서 그 차이가 존재 이유다.
  ⚠️ 이 때문에 `pri`(평평한 목록) 겹침 지표는 오히려 살짝 내려간다(68.0 → 67.5%) — 노라 2장이
  `pri` 한 자리만 채우면서 우리 넷 중 둘을 쓰기 때문이다. **시트 「칸」 기준으로 재야 맞다.**
- **제시·제셀·제로니모를 따로 세는 것은 맞다.** 시트 범례의 `*Jessie = Jessie, Jasser or Jeronimo` 는
  "이 셋 중 아무나"라는 뜻이지 "셋이 한 명"이라는 뜻이 아니다 — 시트 `Rally Joiners` 탭 스스로
  `2xJessie 2xJasser` 를 `4xJessie` 와 **같은 값(2.0)** 으로 적어 뒀다. 서로 다른 연맹원이 각각 데려오는
  별개 조이너이고, 다만 **같은 A칸이라 합연산으로 포화**한다. ⑤ 순위표에 세 줄로 나오는 것도 맞다 —
  누가 자격이 있는지 알아야 하기 때문이다. **묶어서 한 줄로 만들지 말 것.**

영웅을 추가·수정하면 `node test/unit.mjs` 가 `t`/`te`, `p`/`pe` 짝, slot 유효성, 에픽/전설의 스킬 개수를 잡는다.

### 계산 가정 (화면에도 명시돼 있다 — 바꾸면 화면 문구도 같이)

- 원정스킬은 전부 **Lv.5**. **4성이면 원정스킬 만렙**이라 성급은 변수가 아니고 **전무 레벨만** 차등이다.
- 전무 레벨 → %: `2=5 · 4=7.5 · 6=10 · 8=12.5 · 10=15` (`wpct()`)
- 확률·주기 스킬은 **기대값 환산**. 세 가지 꼴이 있고 **X 슬롯도 같은 잣대를 쓴다**
  (2026-08-23 통일 — 예전에는 X 만 원값이 섞여 5턴마다 250%짜리가 상시 250%처럼 계산됐다):
  - 확률형 = 확률 × 값 (`50% 확률 +50%` → .25)
  - 주기형·지속 = 값 × 지속/주기 (`4턴마다 2턴 +30%` → .15)
  - 주기형·단발 = 값 ÷ 주기 (`5공격마다 +100%` → .20)
  100%를 넘는 X 는 대개 환산을 빠뜨린 것이다. 진짜 예외(상시 조건부·감쇠 반영분)는
  `unit.mjs` 의 `BIG_OK` 에 근거와 함께 적는다
- 병종 전용 딜은 **보병 가중치 0.3**을 적용한 딜 지분으로 환산(`DW`) — 보병은 탱커라 병력 비중 대비 딜이 적다
- 일반공격 한정(`An`)은 총딜의 **80%**(`NA_SHARE`)
- **"확률로 N% 피해량"은 A칸에 합산하고, "추가피해·추가공격·N% 피해"는 `AH` 로 빼서 칸 없이 곱한다**
  (2026-09-12). 예전에는 둘을 구분하지 않고 전부 A칸에 넣었는데, 그게 `bk` 규칙과 어긋나는
  비대칭이었다 — 위 정정 이력 참고. 여전히 결과에 가장 큰 영향을 주는 가정이고, 가르는 근거는
  **wosheroes 원문 문구 하나**다

### 감소 계열(20%)은 나눗셈이다 — 세 번째 1차 자료

출처: 볼트 **「랠리 조이너 선정 규칙·개리슨 운영 (Ton)」** (`Google Drive/Obsidian/WOS/`). Ton 영상
*(Gen 1 to 12) Best Rallies to USE to win every battle!* 를 정리한 문서다. 핵심 두 가지:

**§5 — 20% 계열이 밀리는 이유.** 흔한 오해는 "25% 증가(×1.25)와 20% 감소(×0.8)가 상쇄된다"는 것이다.
감소는 곱하는 게 아니라 **나눈다**:

```
1장씩:  ×1.25 ÷ 1.2 = 1.0417   → 증가 쪽 4.17% 우위
4장씩:  ×2.0  ÷ 1.8 = 1.111    → 증가 쪽 11.1% 우위
```

**우리 칸 모델이 이미 이 산수다** — 감소 칸도 같은 `buck` 에 합연산으로 쌓이므로 20% 는 `1.20/1.00 = ×1.200`,
네 장이면 `1.80` 이다. 볼트 모델 `SkillMod = (내 피해증폭 × 적 방어감소) ÷ (적 피해감소 × 적 방어증폭)`
의 **분모**와 정확히 일치한다. **"감소니까 ×0.8 로 바꿔야 한다"고 고치지 말 것 — 그게 그 오해다.**
화면 ⑤ 에 이 설명을 상시 노출한다(`×1.200` 과 `×1.250` 이 "5%p 차이"로 읽히기 때문).

예외도 그대로 옮겼다 — **상대가 같은 영웅을 4스택하면** 상대 칸이 이미 합연산으로 포화라 우리 감소가
상대적으로 크게 먹힌다. 시트 지침 6번("20% skill joiners … can be effective against enemies stacking
4 times the same heroes")의 기전이 이것이다.

**§8 — 공격용/방어용 조이너 구분은 없다.** 데미지 공식이 공수 동일하고 갈리는 건 **위젯**뿐이다.
방어 계열이 대개 20%, 공격 계열이 25% 라서 **방어 상황에서도 25% 증가 계열을 쌓는 쪽이 낫다**(Ton 반복 테스트).

**§6 — 미아는 병종마다 독립 시행이다.** `pc` 필드가 이것이다. 위 정정 이력 참고.
미아 스택 금지도 같은 절에 있다(2번째 미아는 최대 +6.25%p) — 다만 추천 4명이 서로 다른 영웅이라
화면에서는 드러나지 않는다.

### 조이너 동률 타이브레이크 — 투자 문턱으로 가른다

리더가 칸을 채워 놓으면 **빈 칸에 들어가는 25% 스킬이 전부 같은 값**이 된다(실측 9~12명이
×1.250 동률). 값이 같으면 모델은 더 못 가르므로, 아래 "투자 문턱"으로 순서를 정한다:

```
① 배율 내림차순 (6자리 반올림 — 부동소수 끝자리로 동률이 깨지지 않게)
② 시트 등재(s)      — 실제로 조이너로 들어오는 명단
③ 에픽 우선          — 만렙이 싸다
④ 낮은 세대 우선      — 보유율이 높다
⑤ 데이터 순서        — sort 가 ES2019 부터 안정 정렬이라 저절로 지켜진다
```

**②~④ 는 값을 바꾸지 않는다 — 같은 값 안에서만 순서를 정한다.** 예전에는 ②~④ 가 없어
데이터 순서에 맡겼고, 그래서 **그웬(이론·G5)이 헨드릭(시트·G8)을 앞섰다.** 이제 뒤집힌다.

⚠️ **칸 종류로 동률을 가르지 말 것.** 자료가 없어서가 아니라 **공식상 같기 때문**이다.
전투비를 양쪽 식으로 펴면 분자/분모가 상쇄된다:

```
R = SkillMod(나) / SkillMod(상대)
  = (내딜증 ÷ 상대감소) / (상대딜증 ÷ 내감소)
  = (내딜증 × 내감소) / (상대딜증 × 상대감소)
```

**내 딜 칸과 내 감소 칸이 R 에 똑같이 곱해진다.** 그래서 ×1.25 는 어느 칸이든 ×1.25 이고,
딜/생존 가중치를 두면 **틀린다**. (볼트 §8 의 "방어 상황에서도 25% 증가를 쌓는 쪽이 낫다"는
**20% vs 25%** 이야기지 같은 25% 끼리의 우열이 아니다 — 그렇게 읽으면 과대 해석이다.)

같은 이유로 **추천 4명의 요약 숫자는 모든 칸을 곱한 「전투 배율」이다.** 예전에는 딜 칸만
곱해서 생존 칸 조이너의 기여가 0 으로 잡혔다 — Gen10 공격 48/4/48 에서 실제 ×2.344 를
**×1.500** 으로 적고 있었다(패트릭·엘레오노라가 통째로 빠짐). `comboAll()` 이 그 자리다.

### 애매하면 시트를 따른다 — `SHEET_COMPS` · `SHEET_EDGE` (2026-09-12)

이 프로젝트의 1번 원칙("계산이 시트와 엇갈리면 시트를 따른다")을 **엔진에 넣은 것**이다.
그러려면 엔진이 "지금 이 편성이 시트의 어느 행인지"를 알아야 하므로, 시트 편성 행 **53개**를
`src/data.js` 의 `SHEET_COMPS` 로 실었다(예전에는 테스트 픽스처에만 있었다).

```
{g:3, l:[["logan"],["philly"],["zinman"]], rs:[[60,40,0],[60,30,10]],
 j:[["mia"],["patrick"],["jessie","jasser","jeronimo"],["seoyoon"]]}
```

- `l` 은 **리더 대안까지** 배열로 담는다("Ahmose/Logan & Reina & Lynn").
- `rs` 는 라벨에 적힌 병비 **전부**("60/40 or 60/30/10"). 그래서 허용 오차를 넓힐 필요가 없다 —
  `matchComp()` 는 `COMP_TOL=6`(밴드 판정과 같은 값)만 쓴다.
- ⚠️ **`j` 는 평평한 목록이 아니라 「칸」이다.** 시트의 `Jessie*` 는 **한 칸**이고 그 칸의 후보가
  제시/제셀/제로니모 셋이다. 평평하게 펴면 **제시와 제셀을 둘 다 넣고도 "시트대로"** 가 돼서,
  이 기능이 고치려던 문제(같은 칸 중복)를 그대로 놓친다. 실제로 그렇게 만들었다가 안 먹혀서 고쳤다.
  한 명은 **한 칸만** 먹는다.

동작: `pick4()` 가 매 단계에서 최고값의 `1−SHEET_EDGE` 안에 드는 후보 중 **아직 안 채운 시트 칸**을
메우는 쪽을 집는다. **배율을 바꾸지 않는다** — 거의 같은 둘 중 어느 쪽을 집느냐만 정한다.
**맞는 행이 없으면 아무 일도 일어나지 않는다**(보통의 경우다. 이 계산기는 남의 조합 상담용이다).

#### 여유폭 2% 의 근거 — 우리 모델 자체의 오차

임의로 고른 숫자가 아니다. **근거 없는 우리 가정 둘을 흔들어 실측했다**(시트 52행):

| 흔든 것 | 전투 배율 변화 | 추천 명단이 바뀐 행 |
|---|---|---|
| `DW.infantry` 0.3 → 0.5 | 평균 1.75% · 최대 7.01% | 1/52 |
| `DW.infantry` 0.3 → 0.2 | 평균 1.18% · 최대 4.72% | 2/52 |
| `NA_SHARE` 0.8 → 0.7 | 평균 1.04% · 최대 2.44% | **29/52** |
| `NA_SHARE` 0.8 → 0.9 | 평균 1.69% · 최대 2.46% | **35/52** |

`NA_SHARE` 를 ±0.1 흔들면 **절반 넘는 행의 명단이 바뀐다.** 즉 2% 안쪽 차이는 우리가 가를 수 없다.
그 크기 안에서는 계산이 아니라 시트를 따르는 게 맞다. 실측 대가: **평균 −0.323% · 최대 −2.06%**
(여유폭과 같은 크기다). 여유폭을 쓸어 보면 0.5~4% 구간에서 시트 #1 재현이 94.2% 로 **평탄**하고,
5% 부터 손실이 1.17% 로 튄다 — 2% 는 그 평탄 구간 안이다.

#### ⚠️ 시트 우선을 켠 채로 잰 "시트 재현율"은 검증이 아니다

시트를 보고 시트 쪽으로 기울였으니 올라가는 게 당연하다 — **순환논이다.**
그래서 `SHEET_EDGE` 는 최상위 `let` 이고, `unit.mjs` 의 「시트 행 대조」 절은 **`SHEET_EDGE=0` 으로
끄고 순수 모델만 잰다**(92.3% / 61.4%). 시트 우선의 효과(94.2% / 68.0%)는 별도 절에서
**"참고 · 검증 아님"** 으로만 찍고, 거기서 실제로 검사하는 것은 두 가지다:
① 시트에 없는 편성에서는 결과가 안 바뀐다 ② 잃는 배율이 여유폭 근처를 안 넘는다.
**이 두 절을 합치지 말 것.**

### 조이너 선택의 실제 병목 — 투자 문턱

이론 순위보다 **그 영웅이 실제로 Lv.5로 올라와 있는지**가 먼저다. 에픽은 만렙이 싸서 조이너를 지배하고,
만렙 찍힌 전설은 대개 이미 누군가의 리더 팀에 묶여 있어 조이너로 못 뺀다.

**그래서 ⑤ 의 기본 추천은 「시트 등재 영웅만」이다(2026-09-12).** 예전에는 계산 순위 1~4위가 기본이고
시트 명단은 보조 패널이었는데, 시트 52행과 세대별로 대조해 보니 **등재만 쪽이 전 구간에서 같거나 나았다**:

| | #1 재현 | #1~#4 겹침 |
|---|---|---|
| 계산 순위 그대로 | 84.6% (44/52) | 50.0% |
| **시트 등재만 (기본)** | **90.4% (47/52)** | **58.8%** |

**한 행도 나빠지지 않았다** — 세대별로도 Gen 4 80→100% · Gen 8 67→83% · Gen 11 67→100% 로 오르거나 같다.
차이를 만드는 건 이론 배지 영웅 넷이다: **웨인 11회 · 고든 9회 · 플린트 8회 · 그웬 8회** 가 추천에 끼어들고 있었다.
(웨인은 `AH` 분리로 ×1.250 이 된 `Thunder Strike` 때문에 새로 올라온 것이다 — AH 가 노라를 올린 만큼 웨인도 올렸다.)

계산 순위는 **감추지 않고** 바로 아래 `#recAll` 「🧮 계산 순위 그대로」 패널로 계속 보여준다.
두 목록이 같으면 그 패널은 안 나온다. **연맹원이 그 이론 영웅을 이미 만렙으로 갖고 있으면 그쪽이 맞으므로**,
"배율이 틀렸다는 뜻이 아니다"라고 패널에 적어 뒀다.

⚠️ **검사에서 `#rec` 과 `#recAll` 을 헷갈리지 말 것.** 플린트가 `bk` 때문에 밀렸는지 보는 검사를 `#rec`
에서 하면, 플린트는 시트 미등재라 애초에 못 드니 **`bk` 를 도로 빼도 통과한다.** 그 검사는 `#recAll` 을 본다.

---

## 판정 로직 지도 (`src/engine.js`)

`calc()` 가 DOM 을 읽어 순수 계산을 하고 `render(d)` 에 넘긴다. 부분 갱신 없음 —
입력이 바뀌면 `calc()` 전체가 다시 돌고 `#out.innerHTML` 을 통째로 갈아끼운다.

**판정은 `sheetVerdict(en, mine)` 하나뿐이다.** DOM 을 안 만지는 순수 함수라 `unit.mjs` 가
가이드 표 11점을 그대로 먹여 검사한다. 근거는 `COUNTERS`. 원본은 볼트의
`Google Drive/Obsidian/WOS/wos-counter-formation.html` 의 **① 카운터 편성표**다. 시트 행 5개
(+ `src:"theory"` 확장 1행). 상대 비율이 가장 가까운 행과 `ROW_TOL=10`(3차원 유클리드 거리) 안이면
그 행을 적용하고, 내 비율이 금지 항목과 `BAND_TOL=6` 안이면 밴드.
판정 4상태: `ban` / `counter` / `silent`(행은 맞는데 추천에도 금지에도 없음) / `noRow`(맞는 행 자체가 없음).

**행의 `c` 와 `cv` 는 다르다 — 헷갈리면 개수가 안 맞는다.**
`cv` 는 비교용 비율 벡터라 판정이 이것만 보고, `c` 는 화면에 pill 로 뿌리는 표시 라벨이다.
`70/30` 행처럼 가이드의 카운터가 비율이 아니라 전술 서술("멀티랠리")이면 `c` 만 있고 `cv` 는 비어 있다.
그래서 가이드 표 12항목(카운터 7 · 밴드 5) 중 좌표로 비교 가능한 점은 11점이고,
멀티랠리를 전제한 카운터 2건을 빼면 임계값 적합에 쓰인 것은 9점이다.
**허용 오차가 이미 근처 비율을 잡으므로 가이드에 없는 변형을 금지 목록에 덧붙이지 말 것** —
`49/49` 는 `50/50` 에서 거리 2.45라 자동으로 같이 잡힌다.

수비는 `garrisonVerdict(mine, incoming)` — 같은 `COUNTERS` 를 반대 방향으로 조회한다.
`calc()` 가 모드에 따라 둘 중 하나를 부르고, 판정 이름만 다르다
(`counter`↔`threat`, `ban`↔`favorable`, `silent`/`noRow` 는 공통).
`fixtures.mjs` 의 `GARRISON_POINTS` 는 `SHEET_POINTS` 를 뒤집어 만들기 때문에
표가 바뀌면 양쪽이 같이 갱신된다.

---

## 배포

이 저장소는 **main / root** 를 GitHub Pages 로 서빙한다(`.nojekyll` 있음).

```bash
node build.mjs && node test/unit.mjs && node test/browser.mjs
git add -A && git commit -m "..." && git push
```

`index.html` 은 빌드 산출물이지만 Pages 가 그대로 서빙하므로 **커밋 대상이다.** src 만 고치고 빌드를 잊으면
배포가 안 바뀐다 — `unit.mjs` 의 첫 검사가 이걸 잡는다.

배포 확인은 curl + md5 비교로 한다(캐시 때문에 `?cb=` 를 붙인다):

```bash
curl -sL -H "Cache-Control: no-cache" "https://alhena105.github.io/wos-calc/index.html?cb=$RANDOM" -o /tmp/live.html
md5sum /tmp/live.html index.html
```

---

## 작업 규칙

1. **`index.html` 을 직접 고치지 않는다.** `src/*` 를 고치고 `node build.mjs`.
2. **커밋 전 `node test/unit.mjs` 와 `node test/gear-unit.mjs` 를 돌린다.** 판정 로직이나 UI 문구를 건드렸으면 `test/browser.mjs` 도.
3. **새 문자열은 반드시 `L("한글","English")`** 로 감싼다. `unit.mjs` 가 L() 밖의 한글 리터럴을 스캔한다.
   그리고 **언어에 따라 값이 달라지는 최상위 표는 반드시 `i18nFill()` 로 만든다.**
   `const X={...L()...}` 로 두면 로드 시점 언어로 굳어서, `?lang=en` 직행은 멀쩡한데
   **한국어로 열고 English 를 누른 경로에서만** 한글이 남는다. 현재 등록된 표는
   `SLOTS` `tgtName` `CH_NAME` `CH_FIX` `MECH` `COUNTERS`(engine) · `statKr` `PRESETS`(render) 이고,
   `unit.mjs` 가 토글 후 각 표에 한글이 남는지 검사한다(`I18N_TABLES_EXPECTED`).
4. **판정 로직을 바꾸면 `test/fixtures.mjs` 12점이 그대로 통과해야 한다.** 통과 못 하면 로직이 틀린 것이지
   픽스처가 틀린 게 아니다. 픽스처는 시트 원본이 바뀌었을 때만 고친다.
5. 시트 근거가 없는 항목은 화면에 **`이론` 배지**를 붙인다(예: `COUNTERS` 의 `src:"theory"` 행, 수비 판정 전체).
6. 계산 가정을 바꾸면 화면의 **계산 가정 문구(`src/part1.html` `#assump`)** 도 같이 고친다.
7. 커밋 메시지는 한국어 명사형 단문(`시트 판정 4상태 분리`, `병종 메커니즘 정정`).

## 알려진 한계 · 손댈 만한 것

- 시트가 멀티랠리를 전제한 카운터 2건(60/40·60/30/10 행의 40/20/40)은 라벨의 전제 표기가 유일한
  경고다. `unit.mjs` 가 그 라벨이 지워지지 않았는지 본다.
- 조이너 순위표는 `rank.slice(0,14)` 로 14행만 그린다. 사망 판정(배율 1.000)은 맨 뒤로 밀려
  화면에 거의 안 나온다 — "class share too low → dead" 문구를 못 봤다고 버그가 아니다.
- 수비 판정은 표를 뒤집어 읽은 것이라, 랠리 쪽이 다루지 않는 조합은 수비 쪽도 못 다룬다.
- 표가 다루지 않는 비율(`silent`/`noRow`)에는 아무 대안도 못 준다. 지표를 없앤 대가다.
- 영웅 데이터는 Gen 1~17. 새 세대가 나오면 `data.js` 에 추가하고 `#gcap`(서버 최대 세대) 옵션 범위도 늘려야 한다.
- Gen 14~17 열둘은 slot 배정에 **판단이 들어간 자리**가 있다 — 주기·확률형의 기대값 환산과,
  한 스킬이 두 병종에 다르게 걸리는 경우(예: 보병 받피↓ + 창병 딜↑)를 어느 쪽으로 잡을지.
  스킬 원문은 `t`/`te` 에 양쪽 다 적어뒀으니 이견이 있으면 그것부터 볼 것.
- `s:1` 시트 조이너 표시는 2026-09-01 시점 기준. 시트가 갱신되면 `SHEET_JOINERS` 와 같이 맞춘다.
- 추천 조이너 4명은 **시트 등재 영웅 안에서 포화를 보며 한 명씩** 고른다(2026-09-12).
  단, 타격형 X·AH 는 칸이 아니라 자기끼리 포화하지 않는다 — 그 계열이 여럿이면 섞어야 할 이유를 모델이 못 느낀다.
- 남은 이견은 **48/4/48 궁병 공격 4행**뿐이고, 시트는 거기서 노라를 1위로 3장 쌓는다. 우리는 노라를 6~8위로
  놓는데 **차이가 컷과 0.2~3.7%p** 다(G7 0.2%p · G8 0.4%p). "노라가 나쁘다"가 아니라 **4위와 사실상 동률**이라는
  뜻이고, 0.2%p 는 `DW.infantry`·`NA_SHARE` 같은 근거 없는 가정의 오차보다 작다. **시트로는 못 가른다.**
  그리고 **편성 전체로 재면 우리 추천이 시트의 노라 스택보다 3.4~16.9% 높다** — 위 「노라는 특수 케이스」 참고.
  나머지 미스 3행은 전부 제로니모 리더 행인데, **시트가 그 행 주석에 "Jeronimo is Jessie + Seyoon already so
  stacking happens" 라고 스스로 적어 뒀다** — 겹치는 걸 알면서 제시를 1순위로 쓴다. 우리가 맞을 가능성이 높다.
- `DW.infantry = 0.3` 은 **근거 없는 우리 가정이다.** 화면의 병종 스탯 표(공격 보10/창13/사14 ·
  치명 보10/창14/사15)만 보면 보병 단위 딜이 궁병의 1/3까지 떨어지진 않으므로 오히려 낮게 잡은 쪽이다.
  "보병 딜이 과장된다"는 증상은 여기가 아니라 `bk` 누락(2026-09-01)과 `AH` 미분리(2026-09-12)
  때문이었다 — 두 번 다 원인이 칸 귀속이었다. **느낌으로 이 숫자를 만지지 말 것.**
- 시트 `Rally Joiners` 범례에 `Blanchette type of skills (lethality) is also additive is damage up`
  이 있고 표의 Lethality 열에 제시(= 우리 `A` 칸)가 들어가 있다. 파괴력과 피해량이 **같은 칸**이라는
  뜻으로 읽힐 수 있지만, wosheroes 원문은 제시 `Stand of Arms` = damage dealt, 블랑쉬
  `Armed to the Teeth` = Lethality 로 **게임상 다른 스탯**이다. 근거가 비문 한 줄뿐이라 **반영하지 않았다.**
- 시트의 Gen 13~16 행은 비어 있다. 그 세대의 편성·조이너는 **자료 없음**이다.
- 장비: **홍색 전용 XP 표가 원자료에 없다.** `GEAR_XP_BANDS` 는 금장비 곡선 근사라 무료 구간의
  **순서 판단에만** 쓴다. 절대량(며칠 걸리나)은 못 낸다.
- 장비: 능력부여 보너스가 전역인지 조각 한정인지 원자료에 없다 → 상대 비교 전용이다.
- 장비: 마일스톤별 탐험/원정 계열 구분은 게임 내 확인값이고 원자료(성장 가이드 비용표)에는
  스탯 종류만 있다. 패치로 바뀌면 `GEAR_MS` 만 고치면 된다.
